name: deploy-to-development
permissions:
  id-token: write
  contents: read
on:
  push:
    branches:
      - main
jobs:
  deploy-to-development:
    uses: ./.github/workflows/abstractDeploy.yml
    with:
      stage: development
    secrets: inherit
